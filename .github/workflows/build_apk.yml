name: Build Android APK

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    name: Build Signed APKs
    runs-on: ubuntu-latest
    env:
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Decode keystore
        run: |
          mkdir -p android/app
          echo "$ANDROID_KEYSTORE_BASE64" | base64 --decode > android/app/hifdh-release.keystore
          chmod 600 android/app/hifdh-release.keystore

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Get dependencies
        run: flutter pub get

      - name: Extract version
        id: extract_version
        run: |
          VERSION=$(grep 'version: ' pubspec.yaml | sed 's/version: //')
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Build APKs (split per ABI)
        env:
          KEYSTORE_PATH: android/app/hifdh-release.keystore
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          flutter build apk --release --split-per-abi \
            --build-name=$(echo $VERSION | cut -d '+' -f1) \
            --build-number=$(echo $VERSION | cut -d '+' -f2)

      - name: Rename APKs
        run: |
          cd build/app/outputs/flutter-apk/
          for file in app-*-release.apk; do
            ABI=$(echo "$file" | sed 's/app-\(.*\)-release.apk/\1/')
            mv "$file" "../../../../HifdhTracker_${{ env.VERSION }}_${ABI}_release.apk"
          done

      - name: Build universal APK
        env:
          KEYSTORE_PATH: android/app/hifdh-release.keystore
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: flutter build apk --release \
          --build-name=$(echo $VERSION | cut -d '+' -f1) \
          --build-number=$(echo $VERSION | cut -d '+' -f2)

      - name: Rename universal APK
        run: mv build/app/outputs/flutter-apk/app-release.apk "HifdhTracker_${{ env.VERSION }}_universal_release.apk"

      - name: Send APKs via Telegram
        run: |
          for apk in HifdhTracker_*_release.apk; do
            echo "Sending $apk"
            curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
              -F chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
              -F document=@"$apk" \
              -F caption="📦 New build: $apk"
          done

      - name: Upload APK (Universal)
        uses: actions/upload-artifact@v4
        with:
          name: HifdhTracker-universal
          path: "HifdhTracker_*_universal_release.apk"

      - name: Upload APK (armeabi-v7a)
        uses: actions/upload-artifact@v4
        with:
          name: HifdhTracker-armeabi-v7a
          path: "HifdhTracker_*_armeabi-v7a_release.apk"

      - name: Upload APK (arm64-v8a)
        uses: actions/upload-artifact@v4
        with:
          name: HifdhTracker-arm64-v8a
          path: "HifdhTracker_*_arm64-v8a_release.apk"

      - name: Upload APK (x86_64)
        uses: actions/upload-artifact@v4
        with:
          name: HifdhTracker-x86_64
          path: "HifdhTracker_*_x86_64_release.apk"
